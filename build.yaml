# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano
#     shield: reviung41
#   - board: nice_nano
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---

include:
  # Carte : "nice_nano//zmk" depuis le passage au modele materiel v2 de
  # Zephyr. L'ancien "nice_nano_v2" ne correspond plus a rien, et c'est ce
  # qui donnait l'erreur "undefined node label 'nice_view_spi'" : l'overlay
  # de nice_view_adapter, renomme nice_nano_nrf52840_zmk.overlay, n'etait
  # tout simplement jamais charge.
  - board: nice_nano//zmk
    shield: corne_left nice_view_adapter nice_view_gem
    snippet: studio-rpc-usb-uart
  - board: nice_nano//zmk
    shield: corne_right nice_view_adapter nice_view_gem
